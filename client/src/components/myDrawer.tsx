import React from 'react';
import {Text, View} from 'react-native';
import colors from '../assets/colors';

const MyDrawer = () => {
  return (
    <View style={{}}>
      <Text style={{color: colors.black}}>MyDrawer</Text>
    </View>
  );
};

export default MyDrawer;
